pid file = /var/run/rsyncd.pid
lock file = /var/run/rsync.lock
log file = /dev/stdout
port = 873

[test_module]
    path = /chroot
    comment = Test module for backup testing
    read only = no
    list = yes
    uid = 1000
    gid = 1000
