services:
  rsync-ssh:
    image: instrumentisto/rsync-ssh:alpine3.22-r2
    ports:
      - "8873:873/tcp"
    volumes:
      - ./rsyncd.conf:/etc/rsyncd.conf:ro
      - ../../tmp/remote-data:/chroot
    command: "rsync --daemon --no-detach --config=/etc/rsyncd.conf --verbose"
